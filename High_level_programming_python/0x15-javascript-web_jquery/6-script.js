const $ = window.$;
$('DIV#update_header').click(function () {
  $('header').text('New Header!!!');
}
);  // When the user clicks on DIV#update_header, 
// the text of the header must be updated to New Header!!!   